// const fs = require("fs")
// const {
//     runBashCommand,
//     runBashScript,
//     readJsonFile,
//     saveJsonFile
// } = require('./strapux')

module.exports = async function () {
    console.log('post-configuring Strapux')
    // await runBashCommand('npm init -y >/dev/null 2>&1')
    // await runBashScript('bin/bash-command.sh', ['npm', 'install'])
    // let configData = fs.existsSync('strapux.config.json') ? await readJsonFile('./strapux.config.json') : await readJsonFile('./example.strapux.config.json')
    // await saveJsonFile('strapux.config.json', configData)
    // configData = await readJsonFile('./strapux.config.json')

    // // configure frontend and backend paths


    // // remove git, and re-init git,
    // await runBashCommand('rm -rf .git >/dev/null 2>&1')
    // await runBashScript('bin/bash-command.sh', ['git', 'init'])

}
